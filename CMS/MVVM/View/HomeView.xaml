<UserControl x:Class="CMS.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="500" Width="500">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Rectangle Grid.Row="0" Grid.Column="0" Fill="Red" Margin="10" MinWidth="180" MinHeight="180"/>
            <Rectangle Grid.Row="0" Grid.Column="1" Fill="Green" Margin="10" MinWidth="180" MinHeight="180"/>
            <Rectangle Grid.Row="0" Grid.Column="2" Fill="Blue" Margin="10" MinWidth="180" MinHeight="180"/>
            <Rectangle Grid.Row="1" Grid.Column="0" Fill="Yellow" Margin="10" MinWidth="180" MinHeight="180"/>
            <Rectangle Grid.Row="1" Grid.Column="1" Fill="Orange" Margin="10" MinWidth="180" MinHeight="180"/>
            <Rectangle Grid.Row="1" Grid.Column="2" Fill="Purple" Margin="10" MinWidth="180" MinHeight="180"/>
        </Grid>
    </ScrollViewer>
</UserControl>
